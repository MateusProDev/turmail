# Turmail MVP scaffold

Stack: Next.js + TypeScript + Tailwind + Firebase Auth/Firestore + Cloudinary + Brevo + Stripe + BullMQ (worker)

Commands (PowerShell):

```powershell
npm install
npm run dev
npm run worker
```

Configure env vars in `.env.local` as described in `src/lib` files.
# turmail